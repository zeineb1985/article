{% extends 'base.html.twig' %}
{% block title %}Créer un nouveau Article{% endblock %}
{% block body %}
    {% include 'topBar/topBar.html.twig' %}

    <div class="container mt-4 border-primary">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        <div class="form-group">
            {{ form_row(form.title) }}

        </div>
        <div class="form-group">
            {{ form_row(form.content) }}
        </div>
        <div class="form-group">
            {{ form_row(form.date) }}


        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success btn-block"> Ajouter un article</button>

        </div>
        {{ form_end(form) }}

    </div>
{% endblock %}
